nav(class="navbar navbar-default navbar-static-top" ng-controller="NavigationController" id="navigation")
  div(class="container")
    div(class="navbar-header").
      <button type="button" class="navbar-toggle collapsed" ng-init="navbarCollapsed=true" ng-click="navbarCollapsed = !navbarCollapsed" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">Lorelei</a>
    div(class="collapse navbar-collapse" uib-collapse="navbarCollapsed")
      ul(class="nav navbar-nav")
        li #[a(href="/ids/") monitor player progress]
        li(role="separator",class="divider")
        li #[a(href="/tournaments/") view tournament]
        li #[a(href="/tournaments/new") start tournament]

        if extra_links
          for extra_link in extra_links
            li #[a(href="#{extra_link.url}") #{extra_link.text}]

script(type='text/javascript').
  var navigationApp = angular.module('Lorelei', ['ui.bootstrap']);
  navigationApp.controller("NavigationController", ['$scope', function($scope) {}]);
  angular.bootstrap($("#navigation"), ['Lorelei']);
