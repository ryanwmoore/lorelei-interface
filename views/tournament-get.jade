extends tournament

block tournament_content
  if tournament.passwordIsCorrect()
    p.text-info You are a manager of this tournament. You may upload new rounds, change the tournament title, and change the look+feel of the tournament's page.
  script var tid = "#{tid}";
  script var tournament_json_url = "/tournaments/" + "#{tid}" + "/information";
  script var ids_url = "#{ids_get_url}";
  include tournament-get.html
